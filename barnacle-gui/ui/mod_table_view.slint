import { StandardTableView } from "std-widgets.slint";

export global ModTableData {
    in property <[[StandardListViewItem]]> model;
}

export component ModTableView {
    StandardTableView {
        width: 100%;
        height: 100%;
        columns: [
            { title: "Status" },
            { title: "Name" },
        ];
        rows: ModTableData.model;
    }
}
